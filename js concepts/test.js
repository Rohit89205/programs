(function reversed(str){
    // var rev = str.length;
    // console.log(rev);

    // for(let i = 0; i < str.length; i++){
       return str.split('').reverse().join('');
    // }
}).then(
    console.log('OK')
)

// reversed('Rohit').then(console.log('Its ok'));

const data = new Promise(() => {

}).then (
    console.log('OK')
);